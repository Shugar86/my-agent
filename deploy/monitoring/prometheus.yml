global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: my-agent
    static_configs:
      - targets: ["host.docker.internal:8020"]
    metrics_path: /metrics
