# Git/GitHub Skill

Interact with git repositories and GitHub API.

## Tools
- `git_clone` — clone repository to disk
- `git_status` — show working tree status
- `github_list_issues` — list repository issues
- `github_create_issue` — create new issue

Requires: gitpython, PyGithub, GITHUB_TOKEN for GitHub tools
