---
name: voice_io
description: Speech-to-text (Whisper) and text-to-speech (Edge TTS).
version: 1.0
tools:
  - transcribe_audio
  - speak_text
  - list_tts_voices
---

# Voice I/O Skill

- `transcribe_audio` — OpenAI Whisper API (requires `OPENAI_API_KEY`)
- `speak_text` — Edge TTS voices (requires `edge-tts`)
- `list_tts_voices` — list available TTS voices
